import styles from '../styles/Etlap.module.css';

const HeaderAccount = () => (
  <div className={styles.header}>
    <h2 className={styles.normal}>Fiók kezelése</h2>
  </div>
);

export default HeaderAccount;
