import styles from '../styles/Etlap.module.css';

const HeaderElofizetes = () => (
  <div className={styles.header}>
    <h2 className={styles.normal}>Előfizetések</h2>
  </div>
);

export default HeaderElofizetes;
